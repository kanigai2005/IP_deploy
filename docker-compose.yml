services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    restart: always
    env_file:
      - /home/ubuntu/env_config/protostruct3.env

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NEXT_PUBLIC_API_URL=http://16.16.242.152:8000
